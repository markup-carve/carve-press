# Documents the engine this repository INSTALLS renders differently from the
# spec corpus, measured at the pinned freeze.
#
#   corpus  markup-carve/carve 22f7f47, 1259 documents
#   engine  markup-carve/carve-js 2dc3232e426a93ce40ffe3b099c0bb8731fd25e7,
#           as package-lock.json resolves it and `npm ci` builds it
#
# Both ends are fixed, so this list is the whole result rather than a snapshot
# of one. A NAME APPEARING HERE THAT DID NOT BEFORE is this repository's change,
# which in practice means the engine pin a pull request moved made rendering
# worse. A name disappearing is an improvement, recorded by deleting the line.
# A count alone can say neither, because a document regressing while another is
# fixed leaves the total untouched.
#
# THE LIST IS `(none)`, and that is a result rather than a missing record. At the
# previous pin it held 24 names - the 349, 350, 355, 356, 357 and 359 rulings on
# container content columns, table continuation and joined header rows, nested
# quote ends, and footnote definition bodies. Every one of them is carve-js
# implementing the ruling between 099db2cc and the freeze; none was closed here.
# The 14 documents the corpus gained between 0f6b990 and 22f7f47 render
# correctly too, and already did at the previous pin.
#
# An empty list makes this gate strictly stronger, not weaker: any document that
# renders differently from now on is a name that was not here before, which is
# the failing case. `(none)` is spelled out rather than left as an empty file on
# purpose - a truncated or wrongly-pathed file names nothing either, and
# tools/corpus-through-engine.mjs still refuses that. Only the explicit marker
# reads as a measurement.
#
# The corpus ref in .github/workflows/ci.yml, the engine revision in
# package.json, and this list all move together in one change: bump the pin,
# re-measure through the installed engine, and rewrite this file from that run.
(none)
